import React from "react";

type TrendingDataCardsProps = {
  headerImage: string;
  title: string;
  description: string;
};
export const TrendingDataCards: React.FC<TrendingDataCardsProps> = (props) => {
  const { headerImage, title, description } = props;
  return <></>;
};
